import ReactDOM from 'react-dom/client'

import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(<App />)

// refresh();
// counter +=1
// refresh()
// counter +=1
// refresh();
